import styled from 'styled-components';

const Container = styled.div`
  width: 980px;

  img {
    width: 300px;
    width: 280px;
    height: auto;
    outline: none;
  }

  *:focus {
    outline: none;
  }
`;

export default Container;
